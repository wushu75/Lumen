# End-to-end tests

Placeholder for Playwright-based e2e tests of the web app + API. Not yet wired up
in the MVP. Suggested setup: `pnpm add -D @playwright/test` and drive the Vite dev
server + API server together.
