{
  "name": "lumen",
  "version": "0.1.0",
  "private": true,
  "description": "Your open, AI-native second brain.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/wushu75/Lumen.git"
  },
  "packageManager": "pnpm@9.7.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "pnpm --filter @lumen/web dev",
    "dev:api": "pnpm --filter @lumen/api dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "seed": "tsx scripts/seed-demo-vault.ts"
  },
  "devDependencies": {
    "tsx": "^4.16.2",
    "typescript": "^5.5.4",
    "vitest": "^2.0.5"
  }
}
